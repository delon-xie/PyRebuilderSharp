## Phase 47 完成

### 修复内容

| 修复 | 文件 | 效果 |
|:-----|:-----|:------|
| `CleanClassBody` 方法 | `AstBuilder.cs:7550+` | 从类体中移除 `__static_attributes__`, `__classdictcell__`, `__classcell__`, `__firstlineno__`, `return __class__`, trailing `pass` |
| `ExtractClassDef` 过滤 | `AstBuilder.cs:7328+` | 从 `ExtractClassDef` 路径也过滤 class body 伪影 |

### abc.py 3.14 当前输出

```
def abstractmethod(funcobj):                          ✅
class abstractclassmethod(classmethod):               ✅
class abstractstaticmethod(staticmethod):              ✅
class abstractproperty(property):                     ✅
class ABCMeta(type):                                  ✅
def update_abstractmethods(cls):                      ✅
def ABC():                                            ⏳ (应 class ABC(metaclass=ABCMeta))
try:
    pass                                              ⏳ (应 try: from _abc import ...)
```

### 剩余问题（下期）

| 问题 | 说明 | 优先级 |
|:-----|:-----|:------|
| `ABC(metaclass=ABCMeta)` 成 function | `remainingChildCodes` 处理中 base 提取不支持 keyword args | M |
| `try: pass` | ET-based try 体为空（import 语句被跳过） | H |
| `__slots__ = __static_attributes__ = ()` | 链式赋值需处理 | L |
| 文档字符串缩进 | 多行 docstring 续行缩进不对 | L |
