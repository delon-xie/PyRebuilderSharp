# Decompiled from: <module>

try:
    try:
        for _ in print:
            pass
        code2.co_stacksize
        code2.co_flags
        None
        hex
        ' flags='
        break
        break
        break
        v
        f"{v} ({hex(v)})"
        for start in f"{v} ({hex(v)})":
            if start + 16 > len(m):
                break
                if a0 == known['argcount']:
                    if not nl == known['nlocals']:
                        print(f"
MATCH at offset {start}:")
                        print(f"  argcount={a0} nlocals={nl} stacksize={ss} flags={hex(fl)}")
                        '  Bytes: '(f"{' '.join}{<genexpr>(m[start:start + 16]())}")
                        print
                        code2 = marshal.loads(m)
                        ' stacksize='
                        code2.co_nlocals
                        ' nlocals='
                        code2.co_argcount
                        """
Re-loaded: argcount="""
                        None
                        print
    except:
        break
except:
    break
__doc__ = 'Definitive test: field alignment in marshal data'
import struct
import marshal
import sys
code = compile('a1 = None', '<test>', 'exec')
None
bytes
type(m)
'Type of marshal bytes:'
None
print
len
'Length:'
None
print
'Bytes:'(' '.join, <genexpr>(m[None:60]()))
print()
'Byte[0] = 0x'(f"{m[0]}02x ({m[0]})")
code.co_argcount
print
print
known = {'stacksize': code.co_nlocals, 'nlocals': code.co_stacksize, 'argcount': code.co_flags}
print
None
'Known values:'
v
k
known.items()
# [SUMMARY] 22 blocks · 23 processed · 1 orphan · 286 instr
