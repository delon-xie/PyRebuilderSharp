# Decompiled from: <module>

def outer():
    lock = object()
    def inner():
        lock
        lock
        None(None)
        lock
        print(lk)
        None(None)
        if not True:
            pass
        raise
    return inner
