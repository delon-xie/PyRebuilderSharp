# Decompiled from: <module>

with open(INPUT_FILE) as f:
    expected_src = f.read()
    raise
    try:
        ast = ast
        expected_ast = ast.dump(ast.parse(expected_src), indent=2)
    except print:
        sys.exit(1)
# orphan @0x013E
'❌'
# orphan @0x0158
'MISMATCH'
# orphan @0x01A8
'(missing)'
# orphan @0x01C0
'(missing)'
# orphan @0x01FE
ver
results
True
# orphan @0x0262
print(f"
{'========================================'}")
passed = sum(<genexpr>(results.items()))
total = len(results)
'Passed: '(f"{passed}/{total} ({passed / total * 100}{'.0f'}%)")
# [SUMMARY] 34 blocks · 28 processed · 28 orphan · 347 instr
