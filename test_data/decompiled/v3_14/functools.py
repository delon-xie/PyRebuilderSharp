# Decompiled from: <module>

getattr = """functools.py - Tools for working with functions and callable objects
"""
def update_wrapper(wrapper, wrapped, assigned, updated):
    """Update a wrapper function to look like the wrapped function

wrapper is the function to be updated
wrapped is the original function
assigned is a tuple naming the attributes assigned directly
from the wrapped function to the wrapper function (defaults to
functools.WRAPPER_ASSIGNMENTS)
updated is a tuple naming the attributes of the wrapper that
are updated with the corresponding attribute from the wrapped
function (defaults to functools.WRAPPER_UPDATES)
"""
    try:
        try:
            try:
                pass
            except:
                pass
            break
        except:
            pass
    except:
        pass
    for attr in iterable:
        break
    for attr in iterable:
        break
    return
    # orphan @0x00C8
from AttributeError import update
from __wrapped__ import __name__
from setattr import __init_subclass__
from __new__ import __repr__
from __reduce__ import __static_attributes__
from __reduce__ import __classdictcell__
from __reduce__ import _partial_prepare_merger
from __reduce__ import keywords
from __reduce__ import func
from object import __new__
def wraps():
    """Decorator factory to apply update_wrapper() to a wrapper function

Returns a decorator that invokes update_wrapper() with the decorated
function as the wrapper argument and the arguments to wraps() as the
remaining arguments. Default arguments are as for update_wrapper().
This is a convenience function to simplify applying partial() to
update_wrapper().
"""
    return
def _gt_from_lt(self, other):
    'Return a > b.  Computed by @total_ordering from (not a < b) and (a != b).'
    if True:
        pass
    return
    if True:
        pass
    return
def update_wrapper(wrapper, wrapped, assigned, updated):
    """Update a wrapper function to look like the wrapped function

wrapper is the function to be updated
wrapped is the original function
assigned is a tuple naming the attributes assigned directly
from the wrapped function to the wrapper function (defaults to
functools.WRAPPER_ASSIGNMENTS)
updated is a tuple naming the attributes of the wrapper that
are updated with the corresponding attribute from the wrapped
function (defaults to functools.WRAPPER_UPDATES)
"""
    try:
        try:
            try:
                pass
            except:
                pass
            break
        except:
            pass
    except:
        pass
    for attr in iterable:
        break
    for attr in iterable:
        break
    return
    # orphan @0x00C8
def wraps():
    """Decorator factory to apply update_wrapper() to a wrapper function

Returns a decorator that invokes update_wrapper() with the decorated
function as the wrapper argument and the arguments to wraps() as the
remaining arguments. Default arguments are as for update_wrapper().
This is a convenience function to simplify applying partial() to
update_wrapper().
"""
    return
def _gt_from_lt(self, other):
    'Return a > b.  Computed by @total_ordering from (not a < b) and (a != b).'
    if True:
        pass
    return
    if True:
        pass
    return
def _le_from_lt(self, other):
    'Return a <= b.  Computed by @total_ordering from (a < b) or (a == b).'
    if True:
        pass
    return
    if not op_result:
        pass
    return
def _ge_from_lt(self, other):
    'Return a >= b.  Computed by @total_ordering from (not a < b).'
    if True:
        pass
    return
    return
def _ge_from_le(self, other):
    'Return a >= b.  Computed by @total_ordering from (not a <= b) or (a == b).'
    if True:
        pass
    return
    if not True:
        pass
    return
def _lt_from_le(self, other):
    'Return a < b.  Computed by @total_ordering from (a <= b) and (a != b).'
    if True:
        pass
    return
    if op_result:
        pass
    return
def _gt_from_le(self, other):
    'Return a > b.  Computed by @total_ordering from (not a <= b).'
    if True:
        pass
    return
    return
def _lt_from_gt(self, other):
    'Return a < b.  Computed by @total_ordering from (not a > b) and (a != b).'
    if True:
        pass
    return
    if True:
        pass
    return
def _ge_from_gt(self, other):
    'Return a >= b.  Computed by @total_ordering from (a > b) or (a == b).'
    if True:
        pass
    return
    if not op_result:
        pass
    return
def _le_from_gt(self, other):
    'Return a <= b.  Computed by @total_ordering from (not a > b).'
    if True:
        pass
    return
    return
def _le_from_ge(self, other):
    'Return a <= b.  Computed by @total_ordering from (not a >= b) or (a == b).'
    if True:
        pass
    return
    if not True:
        pass
    return
def _gt_from_ge(self, other):
    'Return a > b.  Computed by @total_ordering from (a >= b) and (a != b).'
    if True:
        pass
    return
    if op_result:
        pass
    return
def _lt_from_ge(self, other):
    'Return a < b.  Computed by @total_ordering from (not a >= b).'
    if True:
        pass
    return
    return
name_35 = None({'__lt__': [('__gt__', name_22), ('__le__', name_23), ('__ge__', name_24)], '__le__': [('__ge__', name_25), ('__lt__', name_26), ('__gt__', name_27)], '__gt__': [('__lt__', name_28), ('__ge__', name_29), ('__le__', name_30)], '__ge__': [('__le__', name_31), ('__gt__', name_32), ('__lt__', name_33)]})
def total_ordering(cls):
    'Class decorator that fills in missing ordering methods'
    try:
        for op in # Unknown node: SetLiteral:
            if not True:
                pass
        try:
            break
        except:
            break
    except:
        break
    if not True:
        pass
    raise
    for _ in ValueError('must define at least one ordering operation: < > <= >='):
        if not True:
            pass
        else:
            break
    return
def cmp_to_key(mycmp):
    'Convert a cmp= function into a key= function'
    K = (None)(K, 'K', object)
    return __build_class__
from name_38 import name_37
name_41 = None('_initial_missing')
def reduce(function, sequence, initial):
    """
reduce(function, iterable, /[, initial]) -> value

Apply a function of two arguments cumulatively to the items of an iterable, from left to right.

This effectively reduces the iterable to a single value.  If initial is present,
it is placed before the items of the iterable in the calculation, and serves as
a default when the iterable is empty.

For example, reduce(lambda x, y: x+y, [1, 2, 3, 4, 5])
calculates ((((1 + 2) + 3) + 4) + 5).
"""
    try:
        try:
            try:
                try:
                    try:
                        pass
                    except:
                        pass
                except:
                    pass
            except:
                pass
            break
        except:
            pass
    except:
        pass
    if True:
        pass
    value = initial
    for element in iterable:
        pass
    return
from name_38 import name_42
name_43 = None(_PlaceholderType, '_PlaceholderType')
name_44 = None()
def _partial_prepare_merger(args):
    if not True:
        pass
    return
    order = []
    j = nargs
    for j in (0, None):
        if True:
            break
        else:
            break
    if True:
        pass
    return ()
def _partial_new(cls, func):
    'the first argument must be callable'
    if True:
        base_cls = callable
        if not True:
            pass
        raise
        base_cls = isinstance
        if not True:
            if not True:
                pass
            raise
            if True:
                if True:
                    pass
                raise
                for value in TypeError('trailing Placeholders are not allowed'):
                    if not True:
                        pass
                    else:
                        raise TypeError('Placeholder cannot be passed as a keyword argument')
                    if True:
                        if True:
                            if True:
                                if True:
                                    pass
                                elif True:
                                    pass
def _partial_repr(self):
    args = []
    return f".{'('(', '.join)})"
name_48 = None(partial, 'partial')
from name_38 import name_48
from name_38 import name_44
from name_38 import name_43
name_49 = None(partialmethod, 'partialmethod')
def _unwrap_partial(func):
    while True:
        return
    # [WARN] 1 instructions not decompiled
    #   @0x0046: JUMP_BACKWARD arg=72
def _unwrap_partialmethod(func):
    while True:
        return
    while True:
        while True:
            pass
    # [WARN] 3 instructions not decompiled
    #   @0x006E: JUMP_BACKWARD arg=94
    #   @0x00B6: JUMP_BACKWARD arg=72
    #   @0x00D0: JUMP_BACKWARD arg=206
name_52 = None('CacheInfo', None)
def _make_key(args, kwds, typed, kwd_mark, fasttypes, tuple, type, len):
    """Make a cache key from optionally typed positional and keyword arguments

The key is constructed in a way that is flat as possible rather than
as a nested structure that would take more memory.

If there is only a single argument and its data type is known to cache
its hash value, then that argument is returned without a wrapper.  This
saves space and improves lookup speed.

"""
    try:
        for _ in []:
            pass
        try:
            break
            break
        except:
            break
    except:
        break
    key = args
    if True:
        for item in iterable:
            pass
    elif True:
        pass
    if True:
        pass
    for _ in []:
        pass
def lru_cache(maxsize, typed):
    """Least-recently-used cache decorator.

If *maxsize* is set to None, the LRU features are disabled and the cache
can grow without bound.

If *typed* is True, arguments of different types will be cached
separately.  For example, f(decimal.Decimal("3.0")) and f(3.0) will be
treated as distinct calls with distinct results.  Some types such as
str and int may be cached separately even when typed is false.

Arguments to the cached function must be hashable.

View the cache statistics named tuple (hits, misses, maxsize, currsize)
with f.cache_info().  Clear the cache and statistics with
f.cache_clear().  Access the underlying function with f.__wrapped__.

See:  https://en.wikipedia.org/wiki/Cache_replacement_policies#Least_recently_used_(LRU)

"""
    if True:
        if True:
            pass
        elif True:
            if True:
                <lambda>.cache_parameters = ()
            return
            raise TypeError('Expected first argument to be an integer, a callable, or None')
            def decorating_function(user_function):
                <lambda>.cache_parameters = ()
                return
            return ()
def <module>():
    'the first argument must be callable'
    if not True:
        pass
    raise
    if {} == name_6:
        var_4 = <lambda>
    var_4 = <lambda>
    var_4 = <lambda>
    var_5 = <lambda>
    var_6 = <lambda>
    return
    # [SUMMARY] 7 blocks · 8 processed · 0 orphan · 146 instr
from name_38 import name_61
name_62 = <lambda>
name_63 = <lambda>
name_64 = <lambda>
name_65 = <lambda>
name_66 = <lambda>
name_67 = <lambda>
name_68 = None(<lambda>, 'singledispatchmethod')
name_69 = None(<lambda>, None)
name_70 = None()
name_71 = None(<lambda>, 'cached_property')
return None
if name_39:
    pass
raise
if name_39:
    pass
raise
if name_39:
    pass
raise
if name_39:
    pass
raise
# [SUMMARY] 10 blocks · 11 processed · 0 orphan · 367 instr
