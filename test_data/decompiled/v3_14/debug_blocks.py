# Decompiled from: <module>

try:
    f.read(16)
    code = marshal.load(f)
except:
    pass
try:
    try:
        for _ in sorted_leaders[i + 1]:
            pass
        break
        if not True:
            pass
    except:
        break
except:
    break
import dis
import marshal
import types
import struct
for const in None:
    if not isinstance(const, types.CodeType):
        pass
    break
    if not instr.opname in ('POP_JUMP_IF_FALSE', 'POP_JUMP_IF_TRUE', 'POP_JUMP_IF_FALSE_OR_POP', 'POP_JUMP_IF_TRUE_OR_POP', 'FOR_ITER'):
        pass
    '3d'(f"-{end - 1}3d]: {', '.join}{<genexpr>(block_instrs())}")
    if any is None:
        for last in any is None:
            if not True:
                pass
            last = block_instrs[-1]
            print(f"  → COND: jump_target={last.arg}, fallthrough_offset={block_instrs[-1].offset + 2}")
            if any is None:
                for ins in any is None:
                    for ins in block_instrs:
                        if not True:
                            print(f"  → JUMP: offset={ins.offset}, target={ins.arg}")
                        return None
    if not const.co_name == 'depth_5_while':
        pass
    leaders.add(instr.arg)
    if not i + 1 < len(instrs):
        pass
    print('=== Block structure ===')
    leaders.add(instrs[i + 1].offset)
    for (i, instr) in None:
        pass
    for (i, start) in enumerate(sorted_leaders):
        if i + 1 < len(sorted_leaders):
            end = instrs[-1].offset + 2
        raise
if len(block_instrs) > 3:
    pass
# [WARN] 9 instructions not decompiled
#   @0x00EE: JUMP_BACKWARD arg=0
#   @0x0114: JUMP_BACKWARD arg=0
#   @0x01EE: JUMP_BACKWARD arg=0
#   @0x01F2: JUMP_BACKWARD arg=0
#   @0x0218: JUMP_BACKWARD arg=0
#   @0x0296: JUMP_BACKWARD arg=0
#   @0x03E2: JUMP_BACKWARD arg=700
#   @0x06AC: JUMP_BACKWARD arg=0
#   @0x06B6: JUMP_BACKWARD arg=0
# [SUMMARY] 84 blocks · 85 processed · 8 orphan · 405 instr
